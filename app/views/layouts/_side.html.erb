<nav class="col-sm-3 col-md-2 d-none d-sm-block bg-light sidebar">
  <ul class="nav nav-pills flex-column">
    <% Bucket.all.each do |bucket| %>
      <li class="nav-item">
        <%= active_link_to bucket.name, bucket_items_path(bucket), class: "nav-link" %>
      </li>
    <% end %>
  </ul>
</nav>
